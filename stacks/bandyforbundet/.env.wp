# Per-site variables (place this file as: ./bandyforbundet/.env.wp)
# Duplicate and edit for each site folder.

# Database (unique per site)
WP_DB_NAME=wordpress_bandy
WP_DB_USER=wp_bandy
WP_DB_PASSWORD=changeMeBandy123

# Public host (used by Traefik + WordPress URLs)
WP_DOMAIN=bandyforbundet.localhost

# Optional admin for automated install scripts
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=ChangeMe123!
WP_ADMIN_EMAIL=you@example.com
